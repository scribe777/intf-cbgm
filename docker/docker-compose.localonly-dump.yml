# Scenario override: CBGM_LOCAL_ONLY=true AND CBGM_ALLOW_LOCAL_DUMP=true
# (a non-VMRCRE deploy that still wants its users to load their own dumps).
services:
  cbgm-app-server:
    environment:
      CBGM_LOCAL_ONLY: "true"
      CBGM_ALLOW_LOCAL_DUMP: "true"
